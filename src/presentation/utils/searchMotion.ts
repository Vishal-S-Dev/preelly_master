import { WithSpringConfig } from 'react-native-reanimated';

export const CHIP_PRESS_SCALE = 0.96;

export const chipSpring: WithSpringConfig = {
  damping: 16,
  stiffness: 340,
};

export const PANEL_ANIMATION_MS = 220;
export const PANEL_HEIGHT = 50;
